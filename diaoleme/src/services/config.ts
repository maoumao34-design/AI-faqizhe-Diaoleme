// ============================================================
// 大模型 API 配置
// 把你的 API 地址、密钥、模型名、提示词填到下面，然后重启 dev server
// ============================================================
export const MODEL_API_CONFIG = {
  /** 接口地址 可注册 https://www.SiliconFlow 会有免费额度 */
  url: 'https://api.siliconflow.cn/v1/chat/completions',

  /** API Key */
  apiKey: '',

  /** 支持图文理解的模型 */
  model: 'Qwen/Qwen3-VL-32B-Instruct',

  /**
   * 系统提示词：告诉模型它是什么角色、该怎么分析照片。
   * 可以根据你的模型能力自由调整，只改双引号内的文字即可。
   */
  systemPrompt:
    '你是一名资深的毛发健康顾问，擅长通过掉落头发（枕头、梳子、地漏、水池等场景）判断用户的掉发状态。' +
    '用户会给你一张掉落的头发照片。请根据照片中掉发的数量、粗细、长短、发根是否带白色毛囊等特征，给出以下 JSON 格式的结果（不要返回其他内容）：\n' +
    '{\n' +
    '  "score": 掉发指数 0-100，越高代表掉发量越少、头发越健康,\n' +
    '  "count": "少量|中等|偏多",\n' +
    '  "thickness": "粗硬|正常|细软",\n' +
    '  "suggestions": ["建议1", "建议2", "建议3"]\n' +
    '}\n' +
    '建议要求：1）每条建议一句话 2）具体可执行 3）语气温柔鼓励 4）不要开药方。',

  /** 请求超时（毫秒） */
  timeout: 30000,

  /** 图片传输方式：base64（JSON）或 form（FormData），默认 base64 */
  useBase64: true,
}
